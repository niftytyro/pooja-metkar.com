---
interface Props {
  showDivider?: boolean;
  indent?: "medium" | "large";
}

const { showDivider = true, indent = "medium" } = Astro.props;
---

<header
  class={`flex flex-row justify-between items-center py-8 text-black ${indent === "medium" ? "px-44" : "px-72"} ${showDivider ? "border-b-[1px] border-black120" : ""}`}
>
  <h1 class="font-bold text-xl leading-7">
    <a href="/">POOJA METKAR</a>
  </h1>

  <nav class="text-lg leading-6 space-x-14">
    <a href="/work">WORK</a>
    <a href="/about">ABOUT</a>
  </nav>
</header>
